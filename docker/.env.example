# Required: your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Path on the host to mount as the shared memory volume.
# Both the host agent (Sentinel) and the container agent (Egger) write here.
# Default: ~/agents/memory/
SHARED_MEMORY_HOST_PATH=/home/ubuntu/agents/memory
